# Decisions

## From blueprint (locked before build started)
- Stack: static HTML/CSS/JS only. No frameworks, no build tools, no CMS, no backend. [Source: 01-project.md]
- Hosting: GitHub Pages, auto-deploy from `main`. [Source: 01-project.md]
- Pages: 5 pages (Home, Services, Projects, About, Contact) + 404.html, robots.txt, sitemap.xml. [Source: 10-golden-rules.md]
- Palette: sawdust cream (#F2EDE0), pine-shadow ink (#1C2620), weathered iron rust (#8B3A1F / #6B2A15), concrete gray (#6E6A5F), sagebrush green (#8A8F72). [Source: 03-design.md]
- Fonts: Fraunces (display), Inter (body), Space Mono or JetBrains Mono (utility). No substitutions. [Source: 03-design.md]
- Layout: mobile-first, spacious, max content width 1200px. [Source: 03-design.md]
- Content: all page copy locked verbatim. [Source: 02-pages-content.md]
- Photos: filename-based swaps only, never hardcode paths that break on replacement. [Source: 05-photos.md]
- No scope creep: no contact forms, blog, CMS, analytics, cookie banners, third-party widgets, social embeds unless approved. [Source: 10-golden-rules.md]
- Execution split: build chat proposes, Claude Code executes all file writes. [Source: 10-golden-rules.md]
- Phased workflow: 9 phases, in order, stop for approval between each. [Source: 11-build-workflow.md]

## New decisions made during build
- 2026-08-24 — Phase 1: Used bare working titles (e.g. "Home — Jackson & Grear") for all page `<title>` tags; full SEO titles from 06-seo-social.md deferred to Phases 3–7. Per owner instruction.
- 2026-08-24 — Phase 1 setup: JetBrains Mono selected over Space Mono for utility font. Reason: better legibility at small sizes (nav caps), more distinctive character shape, less overused. Approved by owner. Will be formalized in Phase 2 when style.css is written; 03-design.md remains as-is until then.
- 2026-08-24 — Phase 2: Added `--color-focus: #80856A` token (darkened sagebrush) for focus outlines. Locked `--color-sage` (#8A8F72) measured 2.87:1 against `--color-base`, below the 3:1 WCAG 1.4.11 non-text minimum. New token verified at 3.28:1 on `--color-base` and 4.07:1 on `--color-ink`. Approved by owner.
- 2026-08-24 — Phase 2: Footer links render in `--color-base` text with a `--color-accent` underline (not solid rust text), because rust text directly on the `--color-ink` footer background measured 2.02:1 — well below the 4.5:1 text requirement. Hover/focus flips text color to `--color-accent`. Approved by owner.
- 2026-08-24 — Phase 2: Sticky header uses a solid `--color-base` background with a 1px `--color-secondary` bottom border — no `backdrop-filter`/translucency. Reason: blur reads as a generic modern-web-app pattern, undercuts the "grounded, working outfit" brief. Approved by owner.
- 2026-08-24 — Phase 2: Signature divider — Variant 2 ("stamped ruler ticks") selected over Variant 1 ("chalk-line snap"). Reason: reinforces the "stamped on equipment" aesthetic already established by the mono nav. Variant 1 discarded. Approved by owner. CSS (`.section-divider`) is in place; SVG markup itself is not placed on any page until Phase 3+.
- 2026-08-24 — Phase 2: Desktop nav breakpoint set at 1024px (medium breakpoint). Approved by owner.
- 2026-08-24 — Phase 2: Footer legal line uses `--color-base` at `opacity: 0.6` (rather than a raw hex/rgba) for a cleaner single-token approach. Approved by owner.
- 2026-08-24 — Phase 2: Hamburger menu implemented with a `<button>` + minimal JS (`js/script.js`), not a CSS-only checkbox hack. Reason: native checkboxes only toggle via Space, not Enter, in every major browser, and `07-accessibility.md` locks "Enter or Space" as a requirement. Golden-rules-tier accessibility requirement takes precedence over `08-performance.md`'s softer "prefer CSS if clean" preference. Approved by owner.
- 2026-08-24 — Phase 3: Selected Unsplash photo "Fog rolls in over a misty Idaho mountain morning" (https://unsplash.com/photos/a-view-of-a-forest-with-a-mountain-in-the-background-ooIZbdfybiU) by Alex Moliski, shot in Sawtooth National Forest, Idaho, as the home hero image. Cropped to 16:9, resized to 1920×1080, compressed to 80% JPG quality. Saved as `/images/hero/hero-home.jpg`, final size 244,033 bytes (~238 KB), under the 500 KB budget. Approved by owner (chose Candidate 3 of 3 proposed).
- 2026-08-24 — Phase 3: Hero alt text set to "Morning fog settling over a pine forest and mountain ridge in Idaho's Sawtooth National Forest." — describes the landscape, not the company, per `07-accessibility.md`. Approved by owner.
- 2026-08-24 — Phase 3: Hero built as a real `<img>` (not a CSS `background-image`) layered under a separate `.home-hero__overlay` div at 35% `--color-ink` opacity, with the text content in a third layer on top. Reason: `07-accessibility.md` requires genuine alt text on the hero image and `08-performance.md` requires explicit `width`/`height` attributes and `loading="eager"` — all `<img>`-element concerns rather than CSS-background concerns. Approved by owner.
- 2026-08-24 — Phase 3: Added `fetchpriority="high"` to the hero `<img>` in addition to `loading="eager"`. Not explicitly required by the blueprint, but reinforces `08-performance.md`'s stated loading priority ("Critical: HTML, CSS, hero image"). Approved by owner.
- 2026-08-24 — Phase 3: Service area line rendered as plain body text, not italic, despite markdown italics in `02-pages-content.md`. Reason: read as the source doc's own emphasis notation rather than a locked style directive — same italics appear on Services blurbs with no corresponding style rule in `03-design.md`. Approved by owner.
- 2026-08-24 — Phase 3: "Call for an Estimate" button label written in natural case in HTML; the existing global `.button` rule already applies `text-transform: uppercase` to render it as "CALL FOR AN ESTIMATE" per locked copy. Reason: avoids screen readers spelling out or oddly emphasizing an all-caps string in the source. Approved by owner. Same button markup will be reused verbatim in later phases.
- 2026-08-24 — Phase 3: Designed and placed the first concrete markup for the "Variant 2 — stamped ruler ticks" signature divider (concept only, no markup, was recorded in the Phase 2 entry above). Implemented as an inline SVG: one hairline plus 21 vertical tick marks (long every 5 units, short between), `stroke="currentColor"` inheriting `--color-accent`. Placed once, between the hero and the service-area section. Approved by owner.
- 2026-08-24 — Phase 3: Added `height: 16px` to the existing `.section-divider` rule (style.css Section 8). Reason: the rule had no explicit height, so the newly-placed divider SVG would otherwise render at the browser's default replaced-element height (150px) instead of a thin hairline. No other property in that rule changed. Approved by owner.
- 2026-08-24 — Phase 3: `<link rel="canonical">` and `og:url` left as `[canonical-url]` placeholder per `06-seo-social.md`, resolved per-page in Phase 9; `og:image` set to the real path `/images/hero/hero-home.jpg` (not a placeholder). Confirmed by owner.
- 2026-08-24 — Phase 4: `<h1>` set to "What We Build" rather than a bare repeat of "Services" (already present in the nav link, page title, and section heading) or the vaguer "What We Do." Ties directly to the four categories below (all are things built, poured, framed, or fixed) and to the tagline "Solid construction." Approved by owner.
- 2026-08-24 — Phase 4: Two signature dividers placed (intro→list, list→CTA), reading `03-design.md`'s "used once per section transition on Home and Services" as one marker per transition rather than one marker per page. Home's single divider is consistent with either reading since Home has only one transition; Services has two, so the reading was a genuine judgment call, raised to the owner and approved.
- 2026-08-24 — Phase 4: Services list built as a single column with generous vertical gaps (3rem mobile / 4rem desktop) rather than a 2-column grid. Reason: the four blurbs are not uniform in shape (Remodels & Additions is a noun list; the other three are full sentences), and a grid would force mismatched content into equal-height cells. Single column also keeps the mobile and desktop layouts structurally identical and avoids the templated feature-grid pattern `03-design.md` warns against. Approved by owner.
- 2026-08-24 — Phase 4: Service blurbs rendered as plain paragraph text, not italic, extending the Phase 3 precedent for the service-area line — the italics on 3 of 4 blurbs in `02-pages-content.md` read as the source doc's own emphasis notation rather than a locked style directive (the fourth blurb, Remodels & Additions, isn't italicized at all in the source). Approved by owner.
- 2026-08-24 — Phase 4: `og:image` reuses `/images/hero/hero-home.jpg` — Services has no page-specific photo, and `06-seo-social.md`'s Open Graph block hardcodes that one path as the example for every page. Read as intentional rather than a placeholder gap. Approved by owner.
- 2026-08-24 — Phase 4: No JSON-LD on `services.html`, per `06-seo-social.md` (`LocalBusiness` schema is home-page only).
